on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint
        run: |
          npm ci
          npm run lint

      - name: Build Next.js
        working-directory: ./ui
        run: |
          npm ci
          npm run build
          
      - name: Build AWS
        working-directory: ./infra
        run: |
          npm ci
          npm run build
          npm run test
