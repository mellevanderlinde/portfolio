{
  "name": "reading-time",
  "scripts": {
    "build": "tsc --build --clean",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint-config": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
